[pytest]

testpaths = tests

log_cli=true
log_format = %(asctime)s %(levelname)s %(module)s %(funcName)s::%(lineno)d %(message)s
log_date_format=%Y-%m-%d %H:%M:%S

addopts =
    -s
    -vv
    --tb=long --showlocals